# Sprint 1 (ingestion) needs nothing beyond the Python standard library
# (sqlite3, json, pathlib). The rest of this file lists what upcoming
# sprints will need — install incrementally, not all at once.

pytest>=8.0

# Sprint 2 — IGDB enrichment
requests>=2.31

# Sprint 3 — feature store / NLP review embeddings
sentence-transformers>=2.2
pandas>=2.2
numpy>=1.26

# Sprint 4 — analytics
polars>=1.0
scipy>=1.11

# Sprint 5 — ML
scikit-learn>=1.4
joblib>=1.3
xgboost>=2.0
catboost>=1.2
lightgbm>=4.3
shap>=0.45

# Sprint 6/7 — dashboard & viz
streamlit>=1.35
plotly>=5.20
seaborn>=0.13
matplotlib>=3.8
